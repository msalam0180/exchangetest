{#
/**
 * @file
 * Default view template to display all the fields in a row.
 *
 * Available variables:
 * - view: The view in use.
 * - fields: A list of fields, each one contains:
 *   - content: The output of the field.
 *   - raw: The raw data for the field, if it exists. This is NOT output safe.
 *   - class: The safe class ID to use.
 *   - handler: The Views field handler controlling this field.
 *   - inline: Whether or not the field should be inline.
 *   - wrapper_element: An HTML element for a wrapper.
 *   - wrapper_attributes: List of attributes for wrapper element.
 *   - separator: An optional separator that may appear before a field.
 *   - label: The field's label text.
 *   - label_element: An HTML element for a label wrapper.
 *   - label_attributes: List of attributes for label wrapper.
 *   - label_suffix: Colon after the label.
 *   - element_type: An HTML element for the field content.
 *   - element_attributes: List of attributes for HTML element for field content.
 *   - has_label_colon: A boolean indicating whether to display a colon after
 *     the label.
 *   - element_type: An HTML element for the field content.
 *   - element_attributes: List of attributes for HTML element for field content.
 * - row: The raw result from the query, with all data it fetched.
 *
 * @see template_preprocess_views_view_fields()
 *
 * @ingroup themeable
 */
#}
{% set separator = (attribute(fields, 'field_start_date-revision_id_2').content|render|striptags|trim is not empty) and (attribute(fields, 'field_location-revision_id').content is not empty) ? ' | ' : '' %}
{% set media_title_only_classses = "  media-middle media-title-only" %}
<div class="media-left media-middle">
  <div class="day-month">
    {{ attribute(fields, 'field_start_date-revision_id').content }}
    {{ attribute(fields, 'field_start_date-revision_id_1').content }}
  </div>
</div>
<div class="media-body media-divider {{ media_title_only_classses }}">
  <h3 class="media-heading"><a href="{{ fields.path.content|render|striptags|trim }}">{{ fields.field_short_title.content }}</a></h3>
  <div class="event-details media-details">
    {% if (fields.field_event_type.content|render|striptags|lower != "deadlines") %}
      <div>
        {{ attribute(fields, 'field_start_date-revision_id_2').content }} {{ fields.field_abbreviation.content }}{{ attribute(fields, 'field_location-revision_id').content }}
      </div>
    {% else %}
      <div>
        {{ attribute(fields, 'field_start_date-revision_id_2').content }} {{ fields.field_abbreviation.content }}
      </div>
    {% endif %}
  </div>
</div>
